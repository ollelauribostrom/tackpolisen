/**
 * Exporting Routes
 *
 * @author ollelauribostrom
 */

'use strict';

// Require dependencies
const api   = require('./api');
const admin = require('./admin');

/**
 * Exports
 */
module.exports = {
  api,
  admin
};
